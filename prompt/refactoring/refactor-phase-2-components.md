# Phase 2 상세계획서 - 공통 컴포넌트 구축

## 목표
- 버튼/타이포그래피/배지/모달의 공통 컴포넌트를 타입 안전하게 도입한다.

## 체크리스트
- [x] `AppButton` 컴포넌트 추가 (`variant`, `size`, `isLoading`, 아이콘 슬롯)
- [x] `Typography` 컴포넌트 추가 (`variant`, `tone`, `weight`)
- [x] `Badge` 컴포넌트 추가 (`variant`, `size`)
- [x] `AppModal` 컴포넌트 추가 (ESC 닫기, 포커스 복귀)
- [x] 기존 주요 화면에 `AppButton`/`Badge` 1차 적용

## 완료 기준
- 공통 컴포넌트가 추가되고, 검색/상세 핵심 흐름에서 실제로 사용된다.
